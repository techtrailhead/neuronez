export function Footer() {
  return (
    <footer className="border-t border-white/10 bg-[var(--surface-muted)]/60">
      <div className="mx-auto flex max-w-6xl flex-col gap-3 px-6 py-8 text-sm text-[var(--text-muted)] md:flex-row md:items-center md:justify-between">
        <p className="font-semibold text-[var(--text-primary)]">neuronez LAB</p>
        <p>Tanay & Ankita — building quietly, in public when it matters.</p>
      </div>
    </footer>
  );
}
